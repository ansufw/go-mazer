package pages

import (
    "github.com/ansufw/go-mazer/views/layouts"
	"github.com/ansufw/go-mazer/apps/web/config"
)

templ Home(sidebarItems []config.SidebarItem, filename string) {

    @layouts.Base("Home", "Go Mazer", sidebarItems, filename) {
        "test home"
    }

    
}